<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
<head>
	<meta charset="utf-8" />

	<!-- Uncomment to make IE8 render like IE7 -->
	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

	<!-- Set the viewport width to device width for mobile -->
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	<title><%= content_for?(:title) ? yield(:title) : Rails.env.development? ? "Dev Monopoly Banking" : "Monopoly Banking" %></title>

	<%= stylesheet_link_tag "https://ajax.googleapis.com/ajax/libs/jqueryui/1.7.2/themes/redmond/jquery-ui.css", "application" %>
	<%= javascript_include_tag "vendor/custom.modernizr" %>
	<%= csrf_meta_tags %>
</head>

<body>

	<div id="spinner">
		<%= image_tag image_path('ajax_loader.gif') %>
	</div>

	<%= render 'layouts/navbar' %>

	<%= render partial: 'layouts/flash', locals: { flash: flash } %>

	<div class="row">
		<div class="large-12 columns" role="content">
			<%= yield %>
		</div>
	</div>

	<%= render "layouts/footer" %>

	<%= javascript_include_tag "application" %>
</body>
</html>
